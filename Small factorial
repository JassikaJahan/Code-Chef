#include<stdio.h>
int main(){
    int t,n,mul;
    scanf("%d",&t);
    while(t--){
            mul=1;
        scanf("%d",&n);
        for(int i=1; i<=n; i++){
            mul=mul*i;
        }
        printf("%d\n",mul);
    }
    return 0;
}
